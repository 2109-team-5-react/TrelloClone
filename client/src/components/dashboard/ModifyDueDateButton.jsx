import React from "react";
// import DueDatePopup from "./DueDatePopup";
// import Popover from "../shared/Popover";

const ModifyDueDateButton = () => {
  // const initialPopover = {
  //   type: "due-date",
  //   visible: true
  // };
  // const [popover, setPopover] = useState(initialPopover);
  // setPopover(initialPopover);
  // console.log(setPopover);

  return (
    <>
      <li className="date-button">
        <i className="clock-icon sm-icon"></i>Due Date
      </li>
    </>
  );
};

export default ModifyDueDateButton;
